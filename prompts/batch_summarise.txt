You are a software analysis assistant.
Below is structured information extracted from multiple software repositories.

For EACH repository listed, write an individual summary covering relevant aspects such as:
- The main purpose and goals of the repository
- The type of software (e.g. library, application, tool, framework)
- Key features, capabilities, or use cases
- Dependencies and execution model, if available

Guidelines:
- Write a separate summary for each repository.
- Use ONLY the information provided for each repository. Do NOT use prior knowledge.
- Let the content guide the length of each summary.
- If certain information is missing for a repository, state that briefly.

OUTPUT FORMAT (follow exactly):
For each repository, output:
### <repo_name>
<summary>

---

Repositories:
{{REPOSITORIES}}